# Note: These values are now configured in docker-compose.yml via .env file
# Change SERVER_IP and BACKEND_PORT in the root .env to update these URLs

# Base URL for backend API (server/client safe)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001

# WebSocket URL for live state
NEXT_PUBLIC_WS_URL=ws://localhost:8001/api/v0.1/RAN/UE/live

# Server-only secrets (NO NEXT_PUBLIC_ prefix) — none yet
